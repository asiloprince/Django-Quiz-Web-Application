# Django-Quiz-Web-App
#1 to run the program first we need to install the requirements, go to the terminal and run the command pip install -r requirements.txt
#2 if requirements.txt is not in the directory go to terminal pip freeze > requirements.txt
#3 then repeat #1

#run in the terminal py manage.py runserver


#createsuper user
#run py manage.py createsuperuser/ set up account

